allOf:
  - $ref: ./BaseWebHookEvent.yaml
  - description: Queued Event
    type: object
    properties:
      state:
        type: string
        description: The current state of the job.
        enum:
          - QUEUED
