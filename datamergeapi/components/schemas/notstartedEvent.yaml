allOf:
  - $ref: ./BaseEvent.yaml
  - description: Job is in queued state.
    type: object
    properties:
      status:
        type: string
        description: The current state of the job.
        enum:
          - not_started
