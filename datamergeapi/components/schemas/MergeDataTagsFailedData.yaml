allOf:
  - $ref: ./FailedEvent.yaml
  - description: >-
      Failed Event for Data Merge Tags API. For details of error codes refer to
      ErrorCodesAndDefinitions.
    type: object
    properties:
      data:
        type: object
        properties:
          code:
            type: integer
            description: The error code.
            enum:
              - runtime_error
              - capability_error
              - validation_error
